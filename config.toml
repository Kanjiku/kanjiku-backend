cors_origin = "https://localhost:5173"
app_name = "img_upload_backend"
debug = true
port = 9999
listening_ip = "127.0.0.1"
db_url = "sqlite://test.db"

[Files]
image_path = "./images"
supported_image_types = ["jpeg", "png", "gif", "webp"]

[JWT]
issuer = "kanjiku_backend"
algorithm = "HS256"
secret = "supersecretjwtcode"
